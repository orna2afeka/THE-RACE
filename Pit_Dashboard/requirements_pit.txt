# requirements_pit.txt
# pip install -r requirements_pit.txt
#
# Two pit-side processes:
#   python collector.py            # ingest Firebase -> telemetry.db (run this first)
#   streamlit run pit_dashboard.py # dashboard reads telemetry.db only
streamlit==1.58.0
pandas==2.2.1
numpy==1.26.4
matplotlib==3.8.3
plotly==6.9.0             # History tab overlay chart (zoom/pan/freeze)
pydeck==0.9.3             # Live GPS map deck (ships with Streamlit; pinned since we build the deck ourselves)
openpyxl==3.1.2
requests==2.31.0          # weather_service + collector REST streaming
google-auth==2.29.0       # collector: OAuth2 access token from the service account

# Timezone database for zoneinfo. Windows ships no system tz database, so
# without this the Excel export cannot resolve Asia/Jerusalem or
# Europe/Brussels and raises ZoneInfoNotFoundError. Linux/macOS have one
# already, but the pit laptop is Windows.
tzdata==2026.3
